# Knoxville Area DSA -- new.knoxvilledsa.org
#
# This is a PREVIEW of the chapter site. It is intentionally kept out of every
# search index (regular search AND AI search) by a site-wide
# `X-Robots-Tag: noindex` header (see the _headers file), so it is reachable
# only by direct link and is never mistaken for the live chapter site.
#
# Crawling is deliberately ALLOWED here: a page must be crawlable for the
# noindex header to be seen and honored, so a blanket `Disallow: /` would hide
# the noindex and is the wrong tool. The noindex header does the hiding.
#
# We also opt OUT of AI model training, on this preview and in general.
#
# LAUNCH NOTE: when the site ships on the production chapter domain, the
# intended policy is the reverse of the noindex above: findable in regular
# search AND AI search (so people can find the chapter through ChatGPT and the
# like), while still refusing AI model training. At that point, remove the
# _headers noindex and this preview note; keep the AI-training opt-out below.

User-agent: *
Content-Signal: ai-train=no
Allow: /

# --- AI model-training crawlers: not welcome (preview or production) ---
User-agent: GPTBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: anthropic-ai
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: Amazonbot
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: meta-externalagent
Disallow: /

User-agent: cohere-ai
Disallow: /
